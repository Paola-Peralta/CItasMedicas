import React from "react";
import FormResultado from '../components/FormResultados.jsx';

const AddExamenes = () => {
    return(
        <div>
            <h1>Agregar Resultado</h1>
            <FormResultado/>
        </div>
    );
};

export default AddExamenes;